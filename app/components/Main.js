export const Main = () => {
    let section = document.createElement('section')

    section.id = "section"

    return section
}

/* Crea y retorna un elemento <section> con id="section" 
    const section = document.createElement("section")
    section.id = "section"
    return section
*/